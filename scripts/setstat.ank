addCommand("stat", func(player, args) {
	if len(args) < 2 {
		sendMessage(player, "Invalid args.  Usage: ::stat <idx> <lvl>")
		return
	}

	idx = toInt(args[0])
	lvl = toInt(args[1])
	player.Skillset.SetCur(idx, lvl)
	player.Skillset.SetMax(idx, lvl)
	sendStats(player)
})
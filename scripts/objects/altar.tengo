export func(player, object, cmd) {
	if object.id == 19 || cmd == "Recharge at" {
		// TODO: Set prayer cur stat to prayer max stat, add API for it
		player.message("You recharge your prayer points at the altar.")
		return true
	}
	return false
}
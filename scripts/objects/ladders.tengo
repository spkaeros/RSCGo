isHandled := func(player, object, cmd) {
	return cmd == "climb-up" || cmd == "climb-down"
}

action := func(player, object, cmd) {
	if cmd == "climb-up" {
		player.goUp()
	} else {
		player.goDown()
	}
}